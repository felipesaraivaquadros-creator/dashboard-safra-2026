# Supabase configuration
NEXT_PUBLIC_SUPABASE_URL=https://pohcuxyzdfctfpppnvxa.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_Ri-BgZC1slzklWIYddhvkw_hQVXNxOB

# Optional server key for the keep-alive endpoint. Prefer the new Supabase secret
# key; the legacy service-role key is also supported. Without either one, the
# endpoint uses the public key and performs only RLS-limited reads.
SUPABASE_SECRET_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Recommended in Vercel. Use a random value with at least 16 characters.
CRON_SECRET=
